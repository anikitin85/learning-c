#include <stdio.h>

/*  Exercise 2-2: Write a loop equivalent to this:
    for (i = 0; i < lim - 1 && (c = getchar()) != '\n' && c != EOF; ++i)
        s[i] = c;
    without using && or || */

int main()
{
    
}